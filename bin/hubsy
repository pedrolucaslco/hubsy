#!/bin/bash

# Set Global Variables ---------------------------------------------------------
HUBSY_PATH="$HOME/hubsy"
EDITOR=code

# Prep the terminal ------------------------------------------------------------
clear

# Render Hubsy UI --------------------------------------------------------------
source $HUBSY_PATH/bin/header
source $HUBSY_PATH/bin/menu